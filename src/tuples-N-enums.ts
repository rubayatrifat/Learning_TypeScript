// Tuple
const arr: [number, string] = [3, "rubayat"]; // ite defines fixed data type of array item

// Enums - Enamulations
enum UserRoles {
  ADMIN = "admin",
  GUEST = "guest",
  USER = "user",
}

UserRoles.ADMIN;
