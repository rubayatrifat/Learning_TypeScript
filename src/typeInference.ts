// Type Inference

let name = "Rbayat Rifat" // Automatically choses to be sting

let strOrNum = Math.random() > 0.5 ? 10 : "nothing" // Automatically choses 2 data types


// Type annotaions

let userRole: string = "Backend Developer"
userRole = "System Designer"

